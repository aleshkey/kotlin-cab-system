package org.modsen.passengerservice

object Acronym {
    fun generate(phrase: String) : String {
        var words = phrase.split(" ", "-")
        for (word in words){
            if (word.isNotEmpty()){
                
            }
        }
    }
}
